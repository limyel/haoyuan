sa-token:
  # 不允许从请求体里读取 token
  is-read-body: false
  # 允许从请求头里读取 token
  is-read-header: true
  # 不允许从 cookie 里读取 token，杜绝 csrf 漏洞风险
  is-read-cookie: false
  # token 前缀
  token-prefix: "Bearer"
  token-style: simple-uuid