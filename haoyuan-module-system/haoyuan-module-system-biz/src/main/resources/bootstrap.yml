spring:
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        username: nacos
        password: nacos
        group: sys
        file-extension: yaml
      discovery:
        server-addr: localhost:8848
        username: nacos
        password: nacos
        ephemeral: false
        group: sys
